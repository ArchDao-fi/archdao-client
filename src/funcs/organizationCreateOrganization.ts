/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ArchDAOCore } from "../core.js";
import { appendForm } from "../lib/encodings.js";
import {
  getContentTypeFromFileName,
  readableStreamToArrayBuffer,
} from "../lib/files.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { ArchDaoError } from "../models/errors/archdaoerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import * as operations from "../models/operations/index.js";
import { APICall, APIPromise } from "../types/async.js";
import { isBlobLike } from "../types/blobs.js";
import { Result } from "../types/fp.js";
import { isReadableStream } from "../types/streams.js";

/**
 * Create organization
 *
 * @remarks
 * Creates a new organization (external or ICO type). Uses multipart/form-data for image uploads.
 *
 * **Error Codes:** `VALIDATION_ERROR`, `INVALID_ORG_TYPE`, `INVALID_TOKEN`, `EXPIRED_TOKEN`
 */
export function organizationCreateOrganization(
  client: ArchDAOCore,
  request: operations.CreateOrganizationRequestBody,
  options?: RequestOptions,
): APIPromise<
  Result<
    operations.CreateOrganizationResponseBody,
    | errors.Err
    | ArchDaoError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    request,
    options,
  ));
}

async function $do(
  client: ArchDAOCore,
  request: operations.CreateOrganizationRequestBody,
  options?: RequestOptions,
): Promise<
  [
    Result<
      operations.CreateOrganizationResponseBody,
      | errors.Err
      | ArchDaoError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const parsed = safeParse(
    request,
    (value) =>
      operations.CreateOrganizationRequestBody$outboundSchema.parse(value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = new FormData();

  appendForm(body, "contactInformation", payload.contactInformation);
  appendForm(body, "description", payload.description);
  appendForm(body, "name", payload.name);
  if (payload.banner !== undefined) {
    if (isBlobLike(payload.banner)) {
      appendForm(body, "banner", payload.banner);
    } else if (isReadableStream(payload.banner.content)) {
      const buffer = await readableStreamToArrayBuffer(payload.banner.content);
      const contentType = getContentTypeFromFileName(payload.banner.fileName)
        || "application/octet-stream";
      const blob = new Blob([buffer], { type: contentType });
      appendForm(body, "banner", blob, payload.banner.fileName);
    } else {
      const contentType = getContentTypeFromFileName(payload.banner.fileName)
        || "application/octet-stream";
      appendForm(
        body,
        "banner",
        new Blob([payload.banner.content], { type: contentType }),
        payload.banner.fileName,
      );
    }
  }
  if (payload.external !== undefined) {
    appendForm(body, "external", payload.external);
  }
  if (payload.ico !== undefined) {
    appendForm(body, "ico", payload.ico);
  }
  if (payload.image !== undefined) {
    if (isBlobLike(payload.image)) {
      appendForm(body, "image", payload.image);
    } else if (isReadableStream(payload.image.content)) {
      const buffer = await readableStreamToArrayBuffer(payload.image.content);
      const contentType = getContentTypeFromFileName(payload.image.fileName)
        || "application/octet-stream";
      const blob = new Blob([buffer], { type: contentType });
      appendForm(body, "image", blob, payload.image.fileName);
    } else {
      const contentType = getContentTypeFromFileName(payload.image.fileName)
        || "application/octet-stream";
      appendForm(
        body,
        "image",
        new Blob([payload.image.content], { type: contentType }),
        payload.image.fileName,
      );
    }
  }
  if (payload.links !== undefined) {
    appendForm(body, "links", payload.links);
  }

  const path = pathToFunc("/organization")();

  const headers = new Headers(compactMap({
    Accept: "application/json",
  }));

  const secConfig = await extractSecurity(client._options.bearerAuth);
  const securityInput = secConfig == null ? {} : { bearerAuth: secConfig };
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "createOrganization",
    oAuth2Scopes: [],

    resolvedSecurity: requestSecurity,

    securitySource: client._options.bearerAuth,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "POST",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    errorCodes: ["400", "401", "4XX", "5XX"],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    operations.CreateOrganizationResponseBody,
    | errors.Err
    | ArchDaoError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(201, operations.CreateOrganizationResponseBody$inboundSchema),
    M.jsonErr([400, 401], errors.Err$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
