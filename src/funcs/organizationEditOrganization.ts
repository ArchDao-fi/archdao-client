/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ArchDAOCore } from "../core.js";
import { appendForm, encodeSimple } from "../lib/encodings.js";
import {
  getContentTypeFromFileName,
  readableStreamToArrayBuffer,
} from "../lib/files.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { ArchDaoError } from "../models/errors/archdaoerror.js";
import { Err, Err$inboundSchema } from "../models/errors/err.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import {
  EditOrganizationRequest,
  EditOrganizationRequest$outboundSchema,
  EditOrganizationRequestBody,
  EditOrganizationResponseBody,
  EditOrganizationResponseBody$inboundSchema,
} from "../models/operations/editorganization.js";
import { APICall, APIPromise } from "../types/async.js";
import { isBlobLike } from "../types/blobs.js";
import { Result } from "../types/fp.js";
import { isReadableStream } from "../types/streams.js";

/**
 * Edit organization
 *
 * @remarks
 * Admin-only endpoint to edit organization details.
 *
 * **Error Codes:** `VALIDATION_ERROR`, `ORGANIZATION_NOT_FOUND`, `NOT_AUTHORIZED`, `INVALID_TOKEN`
 */
export function organizationEditOrganization(
  client: ArchDAOCore,
  id: number,
  requestBody?: EditOrganizationRequestBody | undefined,
  options?: RequestOptions,
): APIPromise<
  Result<
    EditOrganizationResponseBody,
    | Err
    | ArchDaoError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    id,
    requestBody,
    options,
  ));
}

async function $do(
  client: ArchDAOCore,
  id: number,
  requestBody?: EditOrganizationRequestBody | undefined,
  options?: RequestOptions,
): Promise<
  [
    Result<
      EditOrganizationResponseBody,
      | Err
      | ArchDaoError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const input: EditOrganizationRequest = {
    id: id,
    requestBody: requestBody,
  };

  const parsed = safeParse(
    input,
    (value) => EditOrganizationRequest$outboundSchema.parse(value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = new FormData();
  if (payload.RequestBody != null) {
    if (payload.RequestBody.banner !== undefined) {
      if (isBlobLike(payload.RequestBody.banner)) {
        appendForm(body, "banner", payload.RequestBody.banner);
      } else if (isReadableStream(payload.RequestBody.banner.content)) {
        const buffer = await readableStreamToArrayBuffer(
          payload.RequestBody.banner.content,
        );
        const contentType =
          getContentTypeFromFileName(payload.RequestBody.banner.fileName)
          || "application/octet-stream";
        const blob = new Blob([buffer], { type: contentType });
        appendForm(body, "banner", blob, payload.RequestBody.banner.fileName);
      } else {
        const contentType =
          getContentTypeFromFileName(payload.RequestBody.banner.fileName)
          || "application/octet-stream";
        appendForm(
          body,
          "banner",
          new Blob([payload.RequestBody.banner.content], { type: contentType }),
          payload.RequestBody.banner.fileName,
        );
      }
    }
    if (payload.RequestBody.description !== undefined) {
      appendForm(body, "description", payload.RequestBody.description);
    }
    if (payload.RequestBody.image !== undefined) {
      if (isBlobLike(payload.RequestBody.image)) {
        appendForm(body, "image", payload.RequestBody.image);
      } else if (isReadableStream(payload.RequestBody.image.content)) {
        const buffer = await readableStreamToArrayBuffer(
          payload.RequestBody.image.content,
        );
        const contentType =
          getContentTypeFromFileName(payload.RequestBody.image.fileName)
          || "application/octet-stream";
        const blob = new Blob([buffer], { type: contentType });
        appendForm(body, "image", blob, payload.RequestBody.image.fileName);
      } else {
        const contentType =
          getContentTypeFromFileName(payload.RequestBody.image.fileName)
          || "application/octet-stream";
        appendForm(
          body,
          "image",
          new Blob([payload.RequestBody.image.content], { type: contentType }),
          payload.RequestBody.image.fileName,
        );
      }
    }
    if (payload.RequestBody.links !== undefined) {
      appendForm(body, "links", payload.RequestBody.links);
    }
    if (payload.RequestBody.name !== undefined) {
      appendForm(body, "name", payload.RequestBody.name);
    }
  }

  const pathParams = {
    id: encodeSimple("id", payload.id, {
      explode: false,
      charEncoding: "percent",
    }),
  };

  const path = pathToFunc("/organization/{id}")(pathParams);

  const headers = new Headers(compactMap({
    Accept: "application/json",
  }));

  const secConfig = await extractSecurity(client._options.bearerAuth);
  const securityInput = secConfig == null ? {} : { bearerAuth: secConfig };
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "editOrganization",
    oAuth2Scopes: [],

    resolvedSecurity: requestSecurity,

    securitySource: client._options.bearerAuth,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "PATCH",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    errorCodes: ["401", "403", "404", "4XX", "5XX"],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    EditOrganizationResponseBody,
    | Err
    | ArchDaoError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, EditOrganizationResponseBody$inboundSchema),
    M.jsonErr([401, 403, 404], Err$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
