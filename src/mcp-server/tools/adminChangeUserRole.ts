/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { adminChangeUserRole } from "../../funcs/adminChangeUserRole.js";
import { ChangeUserRoleRequestBody$inboundSchema } from "../../models/operations/changeuserrole.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  id: z.number().int(),
  requestBody: ChangeUserRoleRequestBody$inboundSchema,
};

export const tool$adminChangeUserRole: ToolDefinition<typeof args> = {
  name: "admin-change-user-role",
  description: `Change user role

Admin-only endpoint to promote or demote users.

**Error Codes:** \`VALIDATION_ERROR\`, \`USER_NOT_FOUND\`, \`NOT_AUTHORIZED\`, \`INVALID_TOKEN\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminChangeUserRole(
      client,
      args.id,
      args.requestBody,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
