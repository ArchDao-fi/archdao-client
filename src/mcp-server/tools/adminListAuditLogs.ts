/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminListAuditLogs } from "../../funcs/adminListAuditLogs.js";
import { ListAuditLogsRequest$inboundSchema } from "../../models/operations/listauditlogs.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListAuditLogsRequest$inboundSchema,
};

export const tool$adminListAuditLogs: ToolDefinition<typeof args> = {
  name: "admin-list-audit-logs",
  description: `List audit logs

Admin-only endpoint to view audit trail of admin actions.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminListAuditLogs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
