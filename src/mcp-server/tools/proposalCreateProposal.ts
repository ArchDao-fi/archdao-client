/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { proposalCreateProposal } from "../../funcs/proposalCreateProposal.js";
import { CreateProposalRequest$inboundSchema } from "../../models/components/createproposalrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateProposalRequest$inboundSchema,
};

export const tool$proposalCreateProposal: ToolDefinition<typeof args> = {
  name: "proposal-create-proposal",
  description: `Create proposal

Creates a new proposal in draft status.

**Error Codes:** \`VALIDATION_ERROR\`, \`INVALID_ACTION_DATA\`, \`EMPTY_ACTIONS\`, \`ORGANIZATION_NOT_FOUND\`, \`INSUFFICIENT_TOKEN_BALANCE\`, \`INVALID_TOKEN\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await proposalCreateProposal(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
