/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationCreateOrganization } from "../../funcs/organizationCreateOrganization.js";
import { CreateOrganizationRequestBody$inboundSchema } from "../../models/operations/createorganization.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateOrganizationRequestBody$inboundSchema,
};

export const tool$organizationCreateOrganization: ToolDefinition<typeof args> =
  {
    name: "organization-create-organization",
    description: `Create organization

Creates a new organization (external or ICO type). Uses multipart/form-data for image uploads.

**Error Codes:** \`VALIDATION_ERROR\`, \`INVALID_ORG_TYPE\`, \`INVALID_TOKEN\`, \`EXPIRED_TOKEN\``,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await organizationCreateOrganization(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
