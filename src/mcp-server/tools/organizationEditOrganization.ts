/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { organizationEditOrganization } from "../../funcs/organizationEditOrganization.js";
import { EditOrganizationRequestBody$inboundSchema } from "../../models/operations/editorganization.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  id: z.number().int(),
  requestBody: EditOrganizationRequestBody$inboundSchema.optional(),
};

export const tool$organizationEditOrganization: ToolDefinition<typeof args> = {
  name: "organization-edit-organization",
  description: `Edit organization

Admin-only endpoint to edit organization details.

**Error Codes:** \`VALIDATION_ERROR\`, \`ORGANIZATION_NOT_FOUND\`, \`NOT_AUTHORIZED\`, \`INVALID_TOKEN\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationEditOrganization(
      client,
      args.id,
      args.requestBody,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
