/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { proposalListProposalStakes } from "../../funcs/proposalListProposalStakes.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  id: z.number().int(),
  page: z.number().int().default(1),
  limit: z.number().int().default(20),
};

export const tool$proposalListProposalStakes: ToolDefinition<typeof args> = {
  name: "proposal-list-proposal-stakes",
  description: `List proposal stakes

Returns a paginated list of stakes for a proposal.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await proposalListProposalStakes(
      client,
      args.id,
      args.page,
      args.limit,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
