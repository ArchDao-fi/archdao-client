/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminListUsers } from "../../funcs/adminListUsers.js";
import { ListUsersRequest$inboundSchema } from "../../models/operations/listusers.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListUsersRequest$inboundSchema,
};

export const tool$adminListUsers: ToolDefinition<typeof args> = {
  name: "admin-list-users",
  description: `List users

Admin-only endpoint to list all users with optional filters.

**Searchable fields:** \`address\`, \`name\`

**Default search field:** \`address\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminListUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
