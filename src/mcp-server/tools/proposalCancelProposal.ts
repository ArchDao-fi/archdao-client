/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { proposalCancelProposal } from "../../funcs/proposalCancelProposal.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  id: z.number().int(),
};

export const tool$proposalCancelProposal: ToolDefinition<typeof args> = {
  name: "proposal-cancel-proposal",
  description: `Cancel proposal

Cancels a proposal. Only allowed before trading begins (draft or staking status).

**Error Codes:** \`PROPOSAL_NOT_FOUND\`, \`INVALID_STATUS_TRANSITION\`, \`NOT_AUTHORIZED\`, \`INVALID_TOKEN\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await proposalCancelProposal(
      client,
      args.id,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
